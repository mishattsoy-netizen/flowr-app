User request: "so what is https://flowr.website/login?????"

0. Date and time of the request:
2026-06-17 23:55

1. User request:
"so what is https://flowr.website/login?????"

2. Objective Reconstruction:
Explain the role of `https://flowr.website/login` as the branded user interface page (entry point) for authentication, where the custom styles and "Sign in with Google" button are shown, as opposed to the dynamic OAuth redirection handlers (Google's accounts site).

3. Strategic Reasoning:
- **Clean Entry Route**: Separate the user interface (the app's login view on `flowr.website/login`) from the identity verification provider (Google's sign-in overlay at `accounts.google.com`).

4. Detailed Blueprint:
- Describe the visual page at `/login`.
- Outline the user transition from `/login` -> Google Accounts -> `/auth/callback` -> `/app`.

5. Operational Trace:
- Documented login route definitions.

6. Status Assessment:
- Diagnostics and solutions provided.
