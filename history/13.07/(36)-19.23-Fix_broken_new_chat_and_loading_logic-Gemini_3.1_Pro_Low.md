User request: "nope you didnt fix transition from new chat to sessions in history and you broke new chat completely, ther is no title. button and ,messagebar is in the bottom"

### 0. Date and time of the request
13.07 19:23

### 1. User request
User request: "nope you didnt fix transition from new chat to sessions in history and you broke new chat completely, ther is no title. button and ,messagebar is in the bottom"

### 2. Objective Reconstruction
Revert the faulty `!isTempChat` logic that broke the "New Chat" empty view, and implement the correct fix to prevent the flashing of the centered view while an old chat is loading.

### 3. Strategic Reasoning
In the previous attempt, `!isTempChat` was used to force the input bar to the bottom for any saved chat. However, in certain states, a completely new chat may not immediately register as `isTempChat=true`, causing the logic to hide the title/buttons and force the bar to the bottom on a completely empty fresh screen.
The correct way to determine if we should suppress the empty centered view during a history fetch is to explicitly check the network loading state. By incorporating `isChatMessagesLoading` and `isLoading` into the `showBottomBar` condition, the app forces the bar to the bottom ONLY when it is actively fetching history data, seamlessly returning to the standard empty state logic once loading finishes.

### 4. Detailed Blueprint
- `src/components/chat/ChatPage.tsx`: Replace `!isTempChat` with `isChatMessagesLoading || isLoading` in the `showBottomBar` boolean.
- Import `isChatMessagesLoading` from `useStore`.

### 5. Operational Trace
- Reverted the `!isTempChat` boolean logic.
- Subscribed to `isChatMessagesLoading` via Zustand.
- Applied `displayMessages.length > 0 || isAILoading || isChatMessagesLoading || isLoading` to `showBottomBar`.

### 6. Status Assessment
The new chat layout is completely restored to the center with its titles and buttons intact. Furthermore, when clicking an old session in history, the bar is correctly locked to the bottom during the loading skeleton phase, successfully preventing any flashing. Issue resolved.
