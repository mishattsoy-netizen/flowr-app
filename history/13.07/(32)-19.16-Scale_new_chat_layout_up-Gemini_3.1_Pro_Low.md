User request: "and can you scale it a little bit in new chat'"

### 0. Date and time of the request
13.07 19:16

### 1. User request
User request: "and can you scale it a little bit in new chat'"

### 2. Objective Reconstruction
Slightly scale up the entire new chat interface group (Greeting, Input Bar, Quick Access Pills) to give it a stronger "hero" presence when centered.

### 3. Strategic Reasoning
Because the title, input bar, and buttons are all absolutely bound to a single wrapper, applying a CSS transform scale to that wrapper scales the entire composition perfectly proportionally from its center. We used a subtle `scale-[1.02]` (2% increase) for the empty state, transitioning smoothly back to `scale-100` when the chat starts.

### 4. Detailed Blueprint
- `src/components/chat/ChatPage.tsx`: Add conditional `scale` classes and a `transition-transform` to the relative wrapper inside the layout container.

### 5. Operational Trace
- Replaced the `<div className="relative w-full flex flex-col items-center">` with a `cn()` dynamic class list.
- Added `transition-transform duration-300`.
- Added `!showBottomBar ? "scale-[1.02]" : "scale-100"`.

### 6. Status Assessment
The new chat layout now features a subtle, smooth scale-up effect when centered, enhancing its visual prominence. Issue resolved.
