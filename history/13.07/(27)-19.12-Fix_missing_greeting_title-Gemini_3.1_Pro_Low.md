User request: "titles dissapeared"

### 0. Date and time of the request
13.07 19:12

### 1. User request
User request: "titles dissapeared"

### 2. Objective Reconstruction
Fix the missing greeting title in empty chats, which became obscured after changing the AI Assistant's layout model to absolute positioning.

### 3. Strategic Reasoning
In the previous fix, the `AIAssistant` was moved to an absolutely positioned container in `ChatPage.tsx` at the vertical center (`top-1/2 -translate-y-1/2`). However, the greeting titles ("Evening, Mikhail Tsoy" and "Write like nobody's listening") were still rendered inside the scrollable container of `ChatConversation.tsx`. Because `ChatConversation`'s centering logic differed slightly from the exact window center, the absolutely positioned `AIAssistant` was rendered perfectly on top of the text, obscuring it.
The most robust fix is to move the greeting title logic entirely into the same absolute wrapper as the `AIAssistant` and the Quick Access Pills, ensuring they render as a single cohesive stack (Title -> Input -> Pills) that centers flawlessly and disappears instantly when the chat starts.

### 4. Detailed Blueprint
- `src/components/chat/ChatConversation.tsx`: Remove `getGreeting`, `user` dependency, and the empty state rendering block.
- `src/components/chat/ChatPage.tsx`: Import the required icons and dependencies (`AIAvatar`, `useAuth`, `MessageCircleDashed`), inject the greeting logic, and render it conditionally directly above the `AIAssistant` when `!showBottomBar`.

### 5. Operational Trace
- Deleted the greeting logic and empty state JSX from `ChatConversation.tsx`.
- Updated `ChatPage.tsx` with the missing imports and state hooks.
- Rendered the title block exactly before `<AIAssistant chatPageMode={true} />` inside the `flex flex-col items-center` container.

### 6. Status Assessment
The greeting titles have been restored and now render perfectly aligned above the `AIAssistant`. The issue is fully resolved.
