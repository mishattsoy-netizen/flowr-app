User request: "setReplyMessage is not defined" (crash report)

### 0. Date and time of the request
13.07 19:17

### 1. User request
User request: "setReplyMessage is not defined"

### 2. Objective Reconstruction
Fix a runtime `ReferenceError` where `setReplyMessage` (and other hooks) were undefined inside `ChatConversation.tsx`.

### 3. Strategic Reasoning
During the initial extraction of the greeting logic out of `ChatConversation.tsx`, several `useStore` hooks (`setReplyMessage`, `regenerateAIMessage`, and `openModal`) were accidentally grouped into the block deletion. Because these functions are still passed down to child components like `ChatMessage` in active chats, triggering or rendering an active chat caused a crash. Restoring those hooks immediately resolves the issue.

### 4. Detailed Blueprint
- `src/components/chat/ChatConversation.tsx`: Re-add the missing `useStore` hooks to the top of the component.

### 5. Operational Trace
- Re-injected `regenerateAIMessage`, `setReplyMessage`, and `openModal` hooks back into `ChatConversation.tsx` after the remaining store subscriptions.

### 6. Status Assessment
The missing dependencies have been restored, resolving the crash immediately. Issue resolved.
