User request: "phase 6"

## Objective Reconstruction
Implement systematic restoration of Phase 6 directive items (Router Configuration & Discovery logic) per the system recovery checklist, rebuilding model layout stability, OpenRouter parameter hardening, and strict Advisor override logic flow controls.

## Strategic Reasoning
Addressing visual degradation in the router component requested explicit dimensional lockings to suppress dynamic reflows. Restoring precision caps avoids decimal overflow glitches in UI rendering, while injecting strict token limits and fallback boolean overrides safeguards runtime cost expenditures by forcing specific behavior contracts from cloud providers without relying on latent upstream default configurations.

## Detailed Blueprint
- Modify global state hooks injecting persistent toggles for commercial pricing filters.
- Implement standalone scalable money formatters ensuring fixed per-million standardizations.
- Rewrite active routing grids locking inline alignment columns with hard-coded zero-collapsing spacers.
- Subsitute legacy standalone power toggles for unified nested-state Options dropdown controllers.
- Inject payload overrides locking max-tokens constants and disabling generic fallback chaining.
- Branch standard intent pipelines hooking classified Advisory triggers directly into internal advisory override interception routines.

## Operational Trace
- Extended `utils.ts` publishing high-precision scaling API constants `formatCostPerMillion`.
- Refactored `DiscoverClient.tsx` syncing conditional visibility constraints against global store and wrapping prompt fields in fixed price formatters.
- Rewrote `RouterManager.tsx` mapping loop deploying static horizontal padding constraints, custom provider selection blocks, and rigid row spacers.
- Substituted dynamic item toggles inside model configuration loop for explicit `RowOptionsDropdown` portal controls.
- Hardcoded JSON fetch payload headers in `openrouter.ts` enforcing `max_tokens: 2000` and `allow_fallbacks: false`.
- Cleansed uncased formatter tokens in `ProviderSelector.tsx` enabling true explicit ID renderings.
- Patched state mutators inside `updateLocalModel` explicitly removing stale key identifiers when moving between distinct infrastructure providers.
- Appended `ADVISOR` categorization definitions in `classifier.ts` permitting recognition by model intelligence feeds.
- Hooked `chainRouter.ts` intercept loop directly processing `runAdvisor` payloads upon detection and dropping down to thinking fallbacks only upon distinct `PASS` signaling.

## Status Assessment
Phase 6 delivers high-compliance consistency for core routing administrative controls. Custom grid scaling fixes regression wrapping and successfully locks element layout density. The system router intelligently guards recursive costs and precisely processes direct advisor intervention triggers. No outstanding Phase 6 items reside at this terminal checkpoint. Full ready status confirmed for subsequent Phase 7 integrity checks.
