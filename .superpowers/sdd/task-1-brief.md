# Task 1: Install `youtube-transcript` npm package

**Files:** `package.json`, `package-lock.json`

**Steps:**
1. Run `npm install youtube-transcript@^1.3.1`
   - Expected: `+ youtube-transcript@1.3.1`
2. Run `git status` to confirm only `package.json` and `package-lock.json` are modified
3. Commit: `git add package.json package-lock.json && git commit -m "chore(deps): add youtube-transcript"`
